<%= form_for :moment, :url=>{:action=> (action), :id=>(id)} do |f|%>
    <b>What message would you like to post?</b><br />
    <%= f.text_area(:message, cols: 50, rows: 6) %><br />
    <%= submit_tag(button_name) %>
<% end %>